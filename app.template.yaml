runtime: nodejs
env: flex

service: api

beta_settings:
  cloud_sql_instances: emvemeapp:us-east4:emve-db

env_variables:
  NODE_ENV: 'production'

automatic_scaling:
  min_num_instances: 2
  max_num_instances: 5

resources:
  memory_gb: 1.2
